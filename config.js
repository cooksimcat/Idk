/**
 * ============================================================
 *  🎨 PETAPP CONFIG — 여기서 이미지 경로와 테마를 바꾸세요
 * ============================================================
 */

window.PET_CONFIG = {

  // ----------------------------------------------------------
  //  테마 색상 (CSS 변수로 적용됨)
  // ----------------------------------------------------------
  theme: {
    bg:           '#fdf6ee',          // 페이지 배경색
    boxBg:        '#fff9f3',          // 메인 박스 배경 (이미지 없을 때)
    boxBorder:    '#e8c9a0',          // 박스 테두리 색
    accent:       '#f4845f',          // 포인트 색 (버튼 등)
    accent2:      '#7ec8c8',          // 스탯바 높을 때 색
    text:         '#3a2e26',          // 기본 글자색
    muted:        '#b89a7a',          // 흐린 글자색
    floorGrad:    '#f5e4cc',          // 바닥 그라디언트 색
  },

  // ----------------------------------------------------------
  //  박스 배경 이미지 (null이면 단색 사용)
  //  예: 'assets/ui/box_bg.png'
  // ----------------------------------------------------------
  boxBgImage: null,

  // ----------------------------------------------------------
  //  박스 테두리 이미지 (null이면 CSS 테두리 사용)
  //  9-slice 느낌으로 border-image에 적용됩니다
  //  예: 'assets/ui/box_border.png'
  // ----------------------------------------------------------
  boxBorderImage: null,

  // ----------------------------------------------------------
  //  액션 버튼 이미지 (null이면 기본 이모지 사용)
  // ----------------------------------------------------------
  actionButtons: {
    sleep: {
      image: null,           // 예: 'assets/actions/btn_sleep.png'
      emoji: '🛏️',
      label: '재우기',
    },
    eat: {
      image: null,           // 예: 'assets/actions/btn_eat.png'
      emoji: '🍖',
      label: '먹이기',
    },
    wash: {
      image: null,           // 예: 'assets/actions/btn_wash.png'
      emoji: '🚿',
      label: '씻기기',
    },
  },

  // ----------------------------------------------------------
  //  액션 연출 이미지
  // ----------------------------------------------------------
  actionScenes: {
    sleep: {
      // 잠자는 장면 이미지 (null이면 기본 연출)
      // 화면 가운데에 표시됨
      image: null,           // 예: 'assets/actions/sleep_scene.png'
    },
    eat: {
      // 음식 이미지 (위에서 떨어짐)
      foodImage: null,       // 예: 'assets/actions/food.png'
      // 먹는 표정 이미지 (petData.images.happy 없을 때 대체)
      // 보통은 아바타 설정의 웃는 표정 사용
    },
    wash: {
      // 샤워기 이미지 (캐릭터보다 크게, 왼쪽에 배치)
      showerImage: null,     // 예: 'assets/actions/shower.png'
      // 커튼 열린 상태 이미지
      curtainOpenImage: null,  // 예: 'assets/actions/curtain_open.png'
      // 커튼 닫힌 상태 이미지
      curtainCloseImage: null, // 예: 'assets/actions/curtain_close.png'
      // 반짝 효과 이미지 (여러 장 쓸 수도 있고 단일 이미지도 OK)
      sparkImage: null,       // 예: 'assets/actions/spark.png'
    },
  },

  // ----------------------------------------------------------
  //  박스 크기 (픽셀)
  // ----------------------------------------------------------
  boxWidth:  380,
  boxHeight: 280,

  // ----------------------------------------------------------
  //  아바타 크기 (픽셀)
  // ----------------------------------------------------------
  avatarSize: 80,
};
